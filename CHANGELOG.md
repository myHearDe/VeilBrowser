# 更新日志

## 0.4.0（相对 0.3.0）

0.4.0 是一次覆盖界面、加密、安全、AdGuard、标签交互和发布流程的完整升级。

### 全新界面与主页

- 新增“午夜翡翠”“瓷白日光”“石墨专注”三套完整主题，可在设置中切换并持久化。
- 石墨专注主题提供适合大量页面的垂直标签布局。
- 重做无边框窗口、标题栏、标签栏、导航栏、资料中心和弹窗布局。
- 修复最大化工作区、Windows 任务栏遮挡、标题栏双击、标签和收藏内容裁切问题。
- 新增本地 `veil.local` 三主题主页，默认入口为午夜主题。
- 新增 UI 设计稿、主题预览和 UI 自动冒烟测试。

### 标签与浏览器基础能力

- 网页弹窗和右键“新窗口打开”统一进入当前窗口的新标签页。
- `Ctrl+N` 按单窗口产品设计创建新标签，而不是额外主窗口。
- 新增复制标签、关闭其他标签、关闭右侧标签和恢复关闭标签。
- 新增 `Ctrl+1..9`、`Ctrl+Shift+Delete` 等常用浏览器快捷键。
- 修复网页视频全屏时系统任务栏置顶和退出后窗口状态恢复问题。
- 收藏支持新增、编辑、取消、打开、复制和删除，并立即加密保存。
- 下载记录区分进行中、完成、取消和其他中断原因；结束后释放事件引用。
- 跟踪防护设置可立即应用到已打开标签；Cookie/WebRTC 参数提示重启生效。

### AdGuard 深度集成

- 保留官方 AdGuard Browser Extension 5.4.3.1，并增加可复现的版本固定和哈希校验下载流程。
- 新增 VeilBrowser 专用 AdGuard 控制中心，修复工具栏页面空白问题。
- 右上角显示实时 `ON/OFF` 状态和 WebView2 Chromium 版本。
- 支持当前网站开关、拦截统计、页面元素选择、自定义规则、过滤日志和完整设置。
- 新增显式标签页元素选择桥接，修复 WebView2 环境下无法选取广告元素的问题。
- 限制控制窗只能使用同一扩展来源，并关闭宿主对象、DevTools 和默认右键菜单。
- 当 WebView2 Runtime 低于 Chromium 121 时给出明确更新提示。

### 加密与本地数据保护

- WebView2 明文工作目录从共享 `%TEMP%` 移至 `%LocalAppData%\VeilBrowser`。
- 为应用数据目录设置仅当前用户、SYSTEM 和 Administrators 可访问的 ACL。
- 浏览器状态、Profile 容器和主密钥包裹改用不同用途的派生子密钥。
- Profile 容器升级为分块 AES-GCM v2，增加认证结束记录，可检测尾随数据和块边界截断。
- 保留受控的 0.3.0/v1 Profile 一次性迁移路径；公开解密接口仍拒绝不带认证结束记录的 v1。
- 旧版状态和主密钥包裹可以兼容读取，并在下一次保存时自动迁移。
- ZIP 恢复拒绝绝对路径、`..` 和目标目录外写入。
- 加密 ZIP 临时文件移出公共临时目录，并执行尽力安全删除。
- 主密码最小长度提升到 12 字符，并要求同时包含字母和数字。
- Argon2id 参数由 3 次迭代提升为 4 次迭代，保持 64 MiB 内存成本。
- DPAPI、密码派生和子密钥处理中的可控临时字节数组执行显式清零。
- 状态保存增加串行化、WriteThrough、显式 Flush 和临时文件原子替换。

### 关闭、崩溃和恢复

- 修复 WebView2 锁文件导致关闭时提示加密失败的问题。
- 使用允许共享读取的流式归档器，跳过易失锁文件和 reparse point，并增加有限重试。
- 加密失败时不再强制退出或删除唯一可恢复数据；恢复标签和界面后允许再次关闭。
- 正常关闭前等待关联 WebView2 子进程释放 Profile。
- 崩溃留下非空工作目录时优先保留较新的可恢复数据，不用旧加密快照覆盖。
- 收藏、密码、历史等资料中心修改立即保存，写入失败时回滚界面状态。

### WebView2 安全边界

- 新窗口仅放行安全 Web 协议，地址栏和导航使用明确允许列表。
- 摄像头、麦克风、位置、通知、剪贴板读取和多文件下载等权限进入用户确认流程。
- 证书错误默认取消，不静默继续访问异常证书页面。
- 禁用网页宿主对象访问；AdGuard 控制窗进一步限制导航与消息来源。

### 安装、签名与发布

- 完善 Inno Setup 安装和卸载向导，可选择保留或彻底清理本地浏览资料。
- 新增人工签名发布脚本：发布程序后暂停等待签名，验证完成后继续生成安装包。
- 安装包签名后再次校验 Authenticode 和 RFC 3161 时间戳，再生成 SHA-256 文件。
- 新增 `SIGNING.zh-CN.md`、签名配置示例和一键打包入口。
- 新增 Windows 10/11 与 WebView2 平台支持说明；明确不以停止维护的 Chromium 109 支持 Windows 7。

### 测试、依赖与文档

- 扩展 Core 测试：AES-GCM 往返、认证 EOF、尾随数据、用途子密钥、旧状态迁移、
  0.3.0 Profile 迁移、ZIP 路径逃逸、锁文件归档和主密码规则。
- 新增 UI 主题资源与 XAML 加载冒烟测试。
- 新增 Dependabot NuGet/GitHub Actions 定期检查。
- 新增平台支持、UI 主题、安全审查、威胁模型和签名发布文档。
- README 新增准确的加密优势、离线破解难度和关键保护边界说明。

### 验证状态

- Release 构建：0 警告、0 错误。
- Core 与 UI 冒烟测试：全部通过。
- NuGet 直接和传递依赖：未发现已知漏洞。
- 实际 GUI 验证：启动、标签快捷键、三主题主页、AdGuard 控制面板和正常关闭加密均通过。
